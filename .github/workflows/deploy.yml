name: Test SSH Connection

on:
  workflow_dispatch:

jobs:
  test-ssh:
    runs-on: ubuntu-latest
    steps:
      - name: 🔑 Test SSH vers VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_COVOITURAGEECORIDE2 }}
          script: |
            echo "✅ CONNEXION SSH RÉUSSIE !"
            echo "Utilisateur: $(whoami)"
            echo "Dossier: $(pwd)"
            echo "=== CONTENU DU DOSSIER BOOKEO ==="
            cd ~/bookeo && ls -la
            echo "=== DOCKER ==="
            docker --version
            echo "✅ TEST COMPLET RÉUSSI !"