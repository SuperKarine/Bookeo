name: Test SSH Connection

on:
  push:
    branches: [ main ]  
  workflow_dispatch:

jobs:
  test-ssh:
    runs-on: ubuntu-latest
    steps:
      - name: 🔑 Test SSH minimal
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_COVOITURAGEECORIDE2 }}
          script: |
            echo "✅ CONNEXION RÉUSSIE - Test minimal"
            whoami
          timeout: 1m